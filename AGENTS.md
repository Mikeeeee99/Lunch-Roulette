# Lunch Roulette AI 项目指南

## 1. 项目概述

- 项目名称：Lunch Roulette AI
- 当前版本：V1.0（`package.json` 中为 `0.1.0`）
- 产品定位：帮助办公室用户快速决定每日午餐的随机转盘 Web App。
- 核心用户：每天午餐时间存在选择困难的办公室用户。
- 核心闭环：查看餐厅 → 自动排除最近吃过的餐厅 → 手动排除今天不想吃的餐厅 → 转动转盘 → 确认结果 → 保存当天记录。
- 当前界面语言：简体中文。
- 目标使用方式：无需登录，直接在浏览器中使用。

用户是 AI 编程初学者。后续修改应保持实现直观、目录清晰、依赖精简，并优先解释关键业务逻辑。

## 2. V1.0 范围

### 已实现

- 首页及产品介绍。
- 12 家本地餐厅数据。
- 餐厅多选排除和重置选择。
- 自动排除最近两个有午餐记录的日期所选择的餐厅。
- 等概率随机选择。
- 动态候选转盘及减速停止动画。
- 结果展示、“换一家”和“就吃这家”。
- 使用 `localStorage` 保存每日午餐记录。
- 首页展示当天结果和最近记录。
- 清空本地记录。
- 响应式布局、键盘焦点样式和减少动画偏好支持。

### 明确暂不实现

- TypeScript。
- Supabase 或其他数据库。
- 用户注册、登录和权限系统。
- AI 推荐或大模型调用。
- 地图、定位或外卖平台接口。
- 后端持久化和多人数据同步。

除非用户明确要求升级版本，不要主动引入以上功能，也不要把当前单页流程改造成复杂的多服务架构。

## 3. 技术栈与运行环境

- React 19。
- JavaScript / JSX，项目业务代码不使用 TypeScript。
- Tailwind CSS 4，通过 PostCSS 加载。
- Vinext + Vite，提供 Next.js App Router 兼容层和 Cloudflare Worker 构建。
- Node.js `>= 22.13.0`。
- ES Modules，`package.json` 包含 `"type": "module"`。
- 数据来源：本地 JSON。
- 持久化：浏览器 `localStorage`。
- 测试：Node.js 内置 `node:test`。

常用命令：

```bash
npm install
npm run dev
npm run build
npm run lint
npm test
```

说明：`npm test` 会先执行完整构建，再运行 `tests/*.test.mjs`。

## 4. 目录和文件职责

```text
app/
├── page.jsx          # 全部页面组件、步骤状态、转盘交互和 localStorage 读写
├── layout.jsx        # 根布局及动态 Open Graph / Twitter 元数据
└── globals.css       # Tailwind 导入、全局颜色、结果动画和无障碍样式

data/
└── restaurants.json  # V1 餐厅数据源

lib/
└── roulette.js       # 可独立测试的日期、候选池和随机选择规则

tests/
├── roulette.test.mjs      # 核心业务规则单元测试
└── rendered-html.test.mjs # 构建产物的服务端渲染冒烟测试

worker/
└── index.js          # Vinext / Cloudflare Worker 入口和图片优化处理

public/
└── og.png            # 社交分享封面，尺寸 1680 × 945
```

其他配置：

- `vite.config.js`：加载 Vinext、Sites 和 Cloudflare 插件；D1、R2 当前均未启用。
- `.openai/hosting.json`：Sites 项目标识及可选资源声明。
- `eslint.config.mjs`：JavaScript、React、Hooks、无障碍和 Next.js 规则。
- `.gitignore`：排除依赖、构建目录、缓存、日志、环境文件和编辑器配置。
- `README.md`：面向使用者的项目介绍和启动方式。

## 5. 页面状态与组件结构

当前应用是单路由、客户端驱动的三步状态机，没有引入路由库：

```text
home → select → wheel
```

- `HomePage`：产品介绍、今日结果、最近记录和开始入口。
- `SelectionPage`：展示餐厅、最近两天自动排除、手动排除和候选数量。
- `WheelPage`：维护重抽排除、转盘角度、旋转状态、结果和确认状态。
- `RouletteWheel`：根据当前候选池生成等分圆盘和餐厅标签。
- `ResultCard`：展示结果、换一家、确认保存和返回首页。
- `AppHeader`：品牌入口和步骤指示。

主要 React 状态：

- `step`：`home`、`select` 或 `wheel`。
- `history`：从浏览器本地存储读取的午餐记录。
- `manualExcluded`：用户本轮手动排除的餐厅 ID。
- `rerollExcluded`：同一转盘会话中“换一家”后暂时排除的餐厅 ID。
- `rotation`：转盘累计旋转角度。
- `isSpinning`：防止动画期间重复触发。
- `selected`：本轮随机选中的餐厅。
- `confirmed`：用户是否点击“就吃这家”。

## 6. 餐厅数据模型

餐厅数据位于 `data/restaurants.json`，每条记录包含：

```json
{
  "id": "r001",
  "name": "老乡鸡",
  "category": "中式快餐",
  "price": 32,
  "distance": "步行 4 分钟",
  "emoji": "🍗",
  "color": "#FFD166",
  "enabled": true
}
```

约束和注意事项：

- `id` 必须唯一且稳定，历史记录通过它关联餐厅。
- `price` 是人均价格数字。
- `distance` 当前是展示字符串，不参与计算。
- `color` 用于转盘扇区和餐厅视觉标识。
- `enabled: false` 的餐厅不会进入候选池。
- 添加餐厅后，首页餐厅数量和转盘会自动更新。
- 修改或删除已有餐厅 ID 会影响历史记录的自动排除规则。
- 当前餐厅池依次为：老乡鸡、林起新烧腊、麦当劳、阿食云南生烫米线、张正发茶餐厅、荷特宝食堂、吉野家、毒蛇面馆、阿拇食记 闽南篇、面霸面馆、牛骨牛肉汤、喜家德水饺。

## 7. 本地历史记录

存储键：

```text
lunch-roulette-history-v1
```

记录结构：

```json
{
  "id": "2026-08-18-r001",
  "date": "2026-08-18",
  "restaurantId": "r001",
  "restaurantName": "老乡鸡",
  "category": "中式快餐",
  "emoji": "🍗",
  "selectedAt": "ISO 时间字符串"
}
```

规则：

- 只有用户点击“就吃这家”后才写入历史。
- 同一天再次确认时，会替换当天旧记录，不会产生多条当天记录。
- 记录按日期降序保存。
- 数据只存在当前浏览器和当前设备中，清除浏览器数据后无法恢复。
- 读取到非法 JSON 时，应用会降级为空历史，不阻塞使用。

## 8. 核心业务规则

核心纯函数位于 `lib/roulette.js`，修改业务规则时优先在此处实现并补充测试。

### 日期格式

`formatDateKey()` 使用本地日历日期生成 `YYYY-MM-DD`，避免 UTC 日期导致跨日偏差。

### 两天不重复

`getRecentRestaurantIds()`：

1. 忽略今天及未来日期。
2. 找到今天之前最近两个“有记录的午餐日”，而不是简单计算最近 48 小时。
3. 返回这两个日期中出现过的餐厅 ID。

例如周四、周五有记录，周末没有记录，则周一仍会排除周四和周五选择的餐厅。

### 候选池

`buildCandidatePool()` 的结果为：

```text
启用的餐厅
- 最近两个用餐日的餐厅
- 用户手动排除的餐厅
- 本轮重抽已经出现过的餐厅
```

排除项统一转成 `Set`，避免重复 ID 影响逻辑。

### 随机选择

`pickRandomRestaurant()` 使用 `Math.random()` 做等概率选择：

- 不使用 AI 决定结果。
- 不使用餐厅权重。
- 空数组返回 `null`。
- 结果必须来自传入候选池。

### 转盘一致性

- 必须先随机确定结果，再计算目标角度并播放动画。
- 指针固定在顶部，转盘旋转至少五圈后停在结果扇区中心。
- 动画时长约为 4.2 秒，4.3 秒后显示结果。
- 动画期间禁止重复点击和返回修改餐厅。
- 不要将“视觉上停到哪里”和“最终结果”分别随机，否则会产生不一致。

### 换一家

- “换一家”会把当前结果加入 `rerollExcluded`，随后重新旋转。
- 同一会话中的餐厅不会在全部候选轮完之前重复出现。
- 如果全部候选都已出现，系统清空重抽排除，并提示餐厅已重新放回转盘。
- 每次得到新结果时，从幽默午餐文案池中随机选择一条，并保证不会与上一条连续重复；文案应鼓励用户接受命运选中的餐厅，减少再次转动，而不是暗示继续纠结。

## 9. 视觉与交互规范

当前品牌风格：温暖、轻松、适合午餐场景。

- 页面背景：奶油色 `#F7F5EF`。
- 主文字和主按钮：深绿色 `#183D31`。
- 主要强调色：橙色 `#FF6B35`。
- 辅助色：黄色 `#FFD166`、薄荷绿 `#70C1A2`。
- 使用粗体、大圆角、轻微立体阴影和食物 Emoji。
- 页面需同时支持手机和桌面浏览器。
- 交互元素需要保留明确的 `focus-visible` 样式和可读的 `aria-label`。
- 新动画必须尊重 `prefers-reduced-motion`。
- 不要为了简单图形引入大型图标库或图片依赖。

## 10. 元数据与部署

- `app/layout.jsx` 根据请求头推导当前域名，生成绝对的 `/og.png` 地址。
- Open Graph 和 Twitter 分享标题、描述应与页面产品文案一致。
- `public/og.png` 是当前品牌分享封面。
- 项目可构建为 Cloudflare Worker 兼容输出。
- `.openai/hosting.json` 中 `d1` 和 `r2` 均为 `null`；V1 不需要数据库或文件存储。
- 本地开发默认地址通常是 `http://localhost:3000/`，以终端实际输出为准。

## 11. 测试与完成标准

修改完成后至少执行：

```bash
npm run lint
npm run build
node --test tests/*.test.mjs
```

如果使用 `npm test`，不需要额外重复执行构建。

业务逻辑变更必须覆盖以下场景：

- 本地日期格式正确。
- 最近两个有记录的用餐日被正确识别。
- 最近、手动、重抽和停用餐厅不会进入候选池。
- 随机结果永远来自候选池。
- 空候选池不会产生无效结果。
- 服务端能够渲染应用外壳和正确的产品元数据。

涉及交互或样式时，还应人工检查：

- 首页 → 餐厅选择 → 转盘 → 结果 → 确认的完整流程。
- “换一家”不会立即重复。
- 确认后刷新页面，今日结果仍存在。
- 手动排除全部餐厅时无法继续。
- 手机宽度下按钮、卡片和转盘不溢出。

## 12. 开发约定

- 优先使用简单 JavaScript，不引入 TypeScript。
- 可测试的业务逻辑放在 `lib/`，不要全部写进 React 组件。
- 餐厅静态数据放在 `data/restaurants.json`，不要散落在组件中。
- 不要把 API 密钥、`.env`、缓存、构建产物或依赖目录提交到 Git。
- 不要直接编辑 `package-lock.json`；依赖变化通过 npm 命令更新。
- 保持 `localStorage` 键和历史数据字段向后兼容；如果必须改变，增加版本化迁移。
- 修改随机算法或转盘角度时，同时验证算法结果与指针位置一致。
- 避免无必要的状态管理库、后端框架和第三方服务。
- 保留现有中文文案风格，面向非技术、快节奏的办公室用户。
- 当前 Git 主分支是 `main`，远程仓库为 `https://github.com/Mikeeeee99/Lunch-Roulette.git`。

## 13. 已知限制

- 数据和历史仅存在当前设备，无法跨设备同步。
- 餐厅信息是静态样例，不会自动获取真实营业状态、距离或位置。
- 当前随机算法不考虑价格、喜好、天气和餐厅权重。
- 当前所有页面组件集中在 `app/page.jsx`；V1 规模可接受，继续显著扩展功能时应再按职责拆分组件。
- 两天不重复规则依赖稳定的餐厅 ID。
- 没有端到端浏览器自动化测试，转盘动画和 localStorage 流程主要依赖人工验收。

## 14. 后续迭代建议

保持循序渐进：

1. 先拆分 `app/page.jsx` 中的组件，保持行为不变。
2. 再增加餐厅编辑或导入能力。
3. 需要跨设备同步时再引入 Supabase 和用户系统。
4. 基础数据闭环稳定后，再考虑自然语言偏好或 AI 推荐。
5. 引入任何新能力时保留“规则筛选 + 程序随机”为可靠核心，AI 只做辅助。
