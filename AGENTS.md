# Lunch Roulette AI 项目指南

## 1. 项目概述

- 当前产品版本：V2.0（`package.json` 为 `0.2.0`）。
- 当前开发状态：V2.0 已通过 GitHub `main` 构建并部署到 Cloudflare Worker；本地仍作为预览和迭代环境，最近一次完整构建、lint 和 36 项自动测试均通过。
- 产品定位：帮助上海办公室用户从附近真实餐厅中快速决定每日午餐。
- 核心闭环：定位 → 距离/预算筛选 → 最近两餐自动排除 → 手动排除 → 最多 30 家转盘 → 确认并保存。
- 当前界面语言：简体中文。
- 无需登录，午餐记录只保存在当前浏览器。

用户是 AI 编程初学者。修改时保持 JavaScript 直观、依赖精简、业务规则可独立测试，并解释关键逻辑。

## 2. V2.0 范围

已实现：

- 首页按钮在用户点击回调内直接请求浏览器定位，兼容 iPhone Safari 权限弹窗；Worker 使用高德 Web 服务 Key 将 WGS84 转为 GCJ-02、解析地址。
- HTTPS、浏览器不支持、Permissions Policy、拒绝、不可用、超时和地址解析失败均有独立错误提示；Safari 已拒绝权限时提供恢复步骤。
- 上海地区限制、逆地理编码和当前位置说明。
- 高德小地图、用户标记、搜索范围、餐厅标记和结果高亮；发现页地图默认折叠，结果地图位于结果弹窗中。
- 5/10/15 分钟近似步行范围，对应 400/800/1200 米。
- 30/40/50 元内或不限预算，默认 40 元内。
- 高德周边餐饮 POI，最多请求两页、返回最多 50 条原始结果。
- 定位后按默认距离后台预加载；距离变化自动请求或复用缓存，预算变化只做本地筛选；发现页不展示餐厅池，点击主按钮后进入排除页。
- 午餐优先过滤会排除咖啡、茶饮、冷饮、甜品、糕饼和酒吧类 POI。
- 发现页只展示定位、距离、预算、地图开关和一个主按钮，不在当前页展示餐厅池或数量；地图默认折叠并按需挂载。
- 严格预算筛选：有限预算排除价格未知餐厅，不限预算保留。
- 最近两个有午餐记录的日期自动排除、手动排除。
- 最多 30 家等概率无放回抽样，不足时全部进入转盘。
- 1–30 家动态转盘、减速动画、餐厅简称、结果弹窗、换一家和结果确认。
- 转盘下方候选清单与扇区使用相同的简称规则，完整名称保留在悬停提示和无障碍标签中；清单完整展开，不使用内部滚动条。
- 转盘使用独立的 30 色扇区色板，相邻扇区及首尾接缝不会同色；标签沿扇区半径倾斜，并根据最终文字角度自动翻转，确保名称按正常方向阅读。
- 转盘根据页面宽高自动适配，桌面最大 900px；圆心是唯一“开始转动”按钮，候选清单在转盘下方使用响应式多列布局。
- localStorage 历史兼容 V1。
- 高德 Web 服务 Key 和 JS 安全密钥只在 Cloudflare Worker 使用。

明确不实现：

- TypeScript、Supabase、用户系统、AI 推荐。
- 餐厅评分、路线规划、导航、地图拖动搜索。
- 手动地址、多城市、外卖平台、餐厅详情页。

## 3. 技术栈

- React 19、JavaScript / JSX、Tailwind CSS 4。
- Vinext + Vite + Cloudflare Worker。
- Next.js App Router 兼容 Route Handlers。
- 高德地图 JS API 2.0、高德 Web 服务 API V3。
- localStorage、Node.js 内置 `node:test`。
- Node.js `>= 22.13.0`、ES Modules。

命令：

```bash
npm install
npm run dev
npm run lint
npm run build
npm test
```

## 4. 主要文件职责

```text
app/page.jsx                         # 四步页面状态、定位搜索、选择、转盘、结果弹窗和历史
app/api/amap-config/route.js         # 向浏览器返回公开 JS Key 与同域代理地址
app/api/nearby-restaurants/route.js  # 附近餐厅服务端入口
app/api/resolve-location/route.js    # 坐标转换与地址解析服务端入口
app/%5FAMapService/[...path]/route.js# /_AMapService 安全代理；编码目录避免被当作私有目录
components/NearbyMap.jsx             # 高德地图与覆盖物
lib/amap-client.js                   # JS SDK 加载、同域位置解析请求
lib/amap-server.js                   # 位置解析、周边搜索、错误隔离和安全代理
lib/discovery.js                     # 距离、预算、上海判断、POI 清洗和展示格式
lib/geolocation.js                   # 浏览器定位、Safari 兼容检查、错误归一化和请求防重
lib/roulette.js                      # 日期、排除、随机、抽样、文案去重和转盘简称规则
tests/*.test.mjs                     # 业务、服务端接口和渲染测试
```

`data/restaurants.json` 是 V1 静态数据，保留作历史参考，不作为 V2 真实搜索结果。

## 5. 页面状态

```text
home → discover → select → wheel
```

- `home`：品牌、今日结果、最近历史。
- `discover`：定位、地图、距离和预算；后台预加载餐厅，点击唯一主按钮后进入排除页。
- `select`：最近两餐自动排除与手动排除。
- `wheel`：最多 30 家候选快照、转盘、换一家、结果弹窗和确认。

相同位置与半径的搜索结果只缓存在 React 页面生命周期内；位置和附近餐厅不写入 localStorage。首页点击会在同一用户手势内立即调用 `getCurrentPosition()`，发现页不得再用 `useEffect` 自动索取权限。定位成功后在后台自动预加载，修改预算只本地筛选，修改距离会自动请求或复用缓存。发现页不展示餐厅列表；主按钮仅在当前条件存在可选餐厅时进入 `select`，加载期间禁用，失败时原位变为重新搜索入口。

## 6. 餐厅模型

高德餐厅统一为：

```json
{
  "id": "高德 POI ID",
  "name": "餐厅名",
  "category": "分类或餐饮服务",
  "address": "地址或空字符串",
  "latitude": 31.2304,
  "longitude": 121.4737,
  "distance": 360,
  "cost": 32,
  "source": "amap",
  "enabled": true,
  "emoji": "🍜",
  "color": "#FFD166"
}
```

- `cost` 缺失或无效时必须为 `null`，不得猜测。
- 不要添加、存储或展示 `rating`。
- `id` 是两天不重复和历史关联的唯一依据，不使用名称模糊匹配。
- `emoji` 和 `color` 根据分类/ID稳定生成，只用于界面。

## 7. 核心规则

- 两天不重复指今天之前最近两个“有午餐记录的日期”。
- 候选顺序：有效 POI → 预算 → 最近两餐 → 手动排除 → 最多 30 家抽样。
- 有效 POI 只包含适合午餐的餐厅；饮品、咖啡、甜品和糕饼门店在服务端清洗阶段排除。
- `sampleCandidates()` 必须等概率、无放回；不超过 30 家时保持全部。
- 进入转盘时创建候选快照，同一轮渲染和换一家不能重新抽样。
- 转盘必须先随机确定结果，再计算指针目标；动画和结果不能分别随机。
- `getWheelLabel()` 会移除门店括号和次要描述，并在有限字符内保留品牌或菜品特征。
- `getWheelSectorColors()` 为最多 30 家候选生成稳定且无相邻重复的转盘配色；餐厅自身 `color` 仍用于卡片和结果弹窗。
- `getWheelLabelLayout()` 计算扇区中心角度和标签翻转方向；应按文字叠加 90° 后的最终方向判断翻转，避免右半边名称反向，同时保持标签随转盘旋转。
- 1–12 家使用最多 10 字标签；13–20 家使用最多 6 字短名；21–30 家使用最多 5 字径向简称，所有候选数量都不使用编号映射；下方清单使用与当前转盘一致的 Emoji 和简称，并通过 `title`、`aria-label` 保留完整名称。
- “换一家”本轮不重复；全部候选转完后恢复完整池。
- 动画约 4.2 秒，结果在约 4.3 秒后出现；每次结果通过 `ResultModal` 弹出，不再插入转盘页面底部。
- 结果弹窗包含餐厅信息、幽默文案、结果地图、“换一家”和“就吃这家”；确认后切换为保存成功状态。
- 结果弹窗打开时锁定背景滚动、约束移动端高度并管理键盘焦点；修改时不得破坏 `role="dialog"`、`aria-modal` 或焦点循环。

## 8. 高德与安全

变量：

```text
NEXT_PUBLIC_AMAP_JS_KEY   # 公开客户端 Key，必须配置域名白名单
AMAP_JS_SECURITY_CODE     # Cloudflare Secret
AMAP_WEB_SERVICE_KEY      # Cloudflare Secret
```

- 敏感值只能保存为 Cloudflare Secret，本地放在被 Git 忽略的 `.env` 或 `.dev.vars`。
- Cloudflare 正式环境必须配置在 `Settings → Runtime variables and secrets`，不能只放在 Build 区域；修改后点击 `Deploy`，并确认新配置版本承接 100% 流量。
- `/_AMapService` 仅允许坐标转换和逆地理编码所需 GET 路径，不得扩展为任意代理。
- `/api/resolve-location` 只接受 POST 和 WGS84 数字坐标，由 Worker 使用 Web 服务 Key 完成转换与逆地理编码。
- `/api/nearby-restaurants` 只接受 POST、GCJ-02、上海坐标和 400/800/1200 半径。
- 高德原始错误、请求 URL、Key 和完整坐标不得写入响应或日志。
- 不使用 Safari UA 嗅探；Permissions API 只能辅助诊断，不能阻止真正的 Geolocation 请求。定位必须由首页或重新定位按钮的点击回调直接触发。
- V2 使用非商业免费额度；额度不足只提示错误，不自动接入付费能力。

## 9. 历史记录

存储键保持：

```text
lunch-roulette-history-v1
```

- 只有点击“就吃这家”后才保存。
- 同一天确认会替换当天旧记录。
- 新记录包含高德 POI ID、名称、分类、价格、距离、地址、`source` 和时间。
- V1 旧记录可继续展示；旧 ID 不强行迁移到同名高德门店。
- 非法 JSON 降级为空历史。

## 10. 完成标准

业务或接口修改后至少运行：

```bash
npm run lint
npm test
```

交互修改还需人工检查：

- 定位成功、拒绝、超时、非上海、重新定位。
- iPhone Safari 首次点击能弹出权限框；此前拒绝时显示网站设置恢复步骤，恢复权限后可重新定位。
- 非 HTTPS、Geolocation 不支持和 `Permissions-Policy: geolocation=()` 分别显示明确错误；Permissions API 异常或误报不得阻止定位调用。
- 5/10/15 分钟和四档预算。
- 发现页不出现餐厅池，主按钮能在搜索完成后直接进入餐厅排除页。
- 价格未知规则、少于/等于/超过 30 家。
- 30 家手机转盘不溢出，扇区简称可辨认且与下方候选清单一致；完整名称可通过悬停提示和无障碍标签获取。
- 2–30 家转盘相邻扇区和首尾接缝不出现同色；标签沿各自色块角度排列，所有名称均按正常的从左到右方向阅读。
- 桌面端大转盘在可用宽高内居中适配，候选清单位于其下方、完整展开且没有内部滚动条。
- 转盘圆心按钮能通过鼠标、触摸和键盘启动，转动时保持固定并禁用重复操作。
- 转盘上和下方清单都不再出现编号映射。
- 每次停止后结果弹窗自动出现，背景不可滚动，键盘焦点停留在弹窗内。
- 换一家会关闭当前弹窗并重新转动，不立即重复；新结果再次弹窗，且指针与结果一致。
- 确认后刷新历史存在，V1 历史仍能读取。
- 无 Key、接口超时、免费额度不足时页面不白屏。

## 11. 开发约定与限制

- 不直接在组件中解析高德原始数据；统一经过 `lib/discovery.js`。
- 不把服务端 Key 传给客户端，不在前端直接调用周边搜索 Web API。
- 不为了新功能引入状态管理库、数据库或大型地图封装库。
- 新动画继续尊重 `prefers-reduced-motion`，交互保留键盘焦点与 aria 文案。
- 发现页保持单一主操作，不重新加入餐厅池、数量面板或多个重复搜索按钮。
- 结果展示继续使用弹窗；如果以后拆分 `app/page.jsx`，应优先抽出 `DiscoverPage`、`RouletteWheel`、`ResultModal` 和 `WheelPage`。
- 当前大部分产品组件仍集中在 `app/page.jsx`；后续继续扩展时再按页面拆分。
- 数据质量依赖高德：人均消费可能缺失，免费额度可能变化，位置是近似值。
- Git 主分支为 `main`，远程仓库为 `https://github.com/Mikeeeee99/Lunch-Roulette.git`。
